# Security Policy

Report vulnerabilities privately through GitHub Security Advisories.

Do not include CFX cookies, GitHub tokens, API secrets, SSH keys, repository credentials, or production logs containing secrets in a public issue.

Only the latest release and the current `main` branch receive security updates.
